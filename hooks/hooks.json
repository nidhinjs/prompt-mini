{
  "hooks": {
    "UserPromptSubmit": [
      {
        "description": "Intercepts vague prompts and routes them to the prompt-mini skill for structured enhancement by asking clarifying questions",
        "hooks": [
          {
            "type": "command",
            "command": "python3 ${CLAUDE_PLUGIN_ROOT}/scripts/evaluate-prompt.py || python ${CLAUDE_PLUGIN_ROOT}/scripts/evaluate-prompt.py",
            "description": "Run the prompt evaluation script"
          }
        ]
      }
    ]
  }
}
